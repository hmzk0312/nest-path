# NestPath AIエージェント作業ガイド

## プロジェクトの目的

NestPathは、現在の資産と将来の収入・支出・運用条件をもとに、長期的な資産推移を月単位でシミュレーションし、二つの退職計画を比較するアプリケーションです。

完成物だけでなく、AIを活用しながら設計、実装、テスト、CI/CD、ドキュメンテーションの進め方を学ぶことを重視します。設計や実装を行う場合は、採用した方法とその理由を説明してください。

## 要件・仕様の参照先

作業を開始する前に、作業内容に応じて次の情報を確認してください。

* `README.md`

  * プロジェクトの目的、MVPの範囲、MVP対象外、全体の受け入れ条件を確認する
* `docs/product/user-stories.md`

  * MVPのUser Story一覧、各User Storyの責務、依存関係、Issue化状況を確認する
* `docs/calculation-rules.md`

  * 月次シミュレーションの処理順序、計算式、税、売却、現金不足、資産枯渇などの計算規則を確認する
* 対応するGitHub Issue

  * 個別User StoryまたはTaskのスコープ、受け入れ条件、テスト観点、依存関係、完了条件を確認する
* 現在のソースコードとテスト

  * 既存の設計、実装済みの振る舞い、変更による影響範囲を確認する

各情報の役割は次のように区別してください。

* `README.md`は、MVP全体の目的と範囲を管理する
* `docs/product/user-stories.md`は、User Story一覧と依存関係を管理する
* `docs/calculation-rules.md`は、横断的な計算規則を管理する
* GitHub Issueは、個別User Storyおよび実装Taskの詳細を管理する
* ソースコードとテストは、現在実装されている振る舞いを示す

文書、Issue、ソースコード、テストの間に矛盾や未決定事項がある場合は、独自に解釈して確定または実装せず、該当箇所と影響を報告してユーザーへ確認してください。

個別Issueだけを根拠に、READMEに定義されたMVPの範囲を拡張しないでください。

## 作業方針

* 現在の目的を満たすシンプルな方法を優先し、YAGNIを意識する
* 将来の可能性だけを理由に、設計や実装を複雑化しない
* MVP対象外の案は現在の作業へ混ぜず、必要に応じて将来候補として分離する
* 大きな変更は、小さく実装・テスト・レビューできる単位へ分割する
* 各段階で、完了条件を客観的に確認する
* 新しいアーキテクチャ、フレームワーク、ライブラリ、エラー型などは、現在のTaskに必要な場合だけ検討する
* 未決定の技術事項を、実装上必要になる前に先回りして決定しない
* 既存の設計や命名を変更する場合は、変更理由と影響範囲を説明する
* 実際の資産額、収入、年金見込額などの個人データをコミットしない
* サンプルとテストには架空のデータを使用する
* 認証情報や秘密情報をコミットしない

## User StoryとTaskの扱い

* User Storyは、利用者またはMVPにとっての価値を表す要求単位として扱う
* Taskは、User Storyを実現するための実装・テスト単位として扱う
* User Storyが一度に実装・テスト・レビューするには大きい場合は、親User Storyを維持したままTaskへ分割する
* Taskを分割するときは、親Issue、依存関係、推奨する実装順を明確にする
* 受け入れ条件をTaskへ割り当てる場合は、漏れや不必要な重複がないことを確認する
* GitHub Issueを作成する前に、既存Issueとの重複を確認する
* 実装を開始する前に、依存するIssueと実装開始の前提条件が満たされていることを確認する

## 依頼範囲の遵守

依頼された作業範囲を超えて、次の工程へ自動的に進まないでください。

* 調査を依頼された場合は、調査結果を提示した時点で停止する
* 要件整理や設計を依頼された場合は、案を提示した時点で停止する
* Issue本文の作成を依頼された場合は、本文案を提示した時点で停止する
* Issue登録を依頼された場合は、指定されたIssueの登録と結果報告が完了した時点で停止する
* 実装計画を依頼された場合は、計画を提示した時点で停止する
* 実装を依頼された場合だけ、ファイルの作成・変更と必要なテストを行う

次の工程へ進む必要がある場合は、ユーザーからの明示的な指示を待ってください。

## GitHub操作

GitHubへの書き込み操作は、ユーザーから明示的に依頼された場合だけ行ってください。

書き込み操作には、次のものを含みます。

* Issueの作成または更新
* コメントの追加
* ラベル、マイルストーン、GitHub Projectの変更
* ブランチ作成
* コミット
* push
* Pull Requestの作成または更新
* その他のGitHub上の変更

GitHub操作を依頼された場合も、指定された対象と操作だけを行い、完了後に実行結果を報告してください。
